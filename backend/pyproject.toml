[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiofiles>=25.1.0",
    "fastapi[standard]>=0.139.0",
    "python-magic>=0.4.27",
    "sqlmodel>=0.0.39",
]

[tool.ty.environment]
python=".venv"
